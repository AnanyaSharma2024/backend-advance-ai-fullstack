services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - 8000:5000

  frontend:
    build: ./frontend
    ports:
      - 5173:5173

  redis:
    image: redis:alpine
    ports:
      - 6379:6379 